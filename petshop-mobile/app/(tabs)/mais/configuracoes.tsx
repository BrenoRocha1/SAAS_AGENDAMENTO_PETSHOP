import { EmBreve } from '@/components/EmBreve'

export default function ConfiguracoesScreen() {
  return (
    <EmBreve
      titulo="Configurações"
      icone="settings-outline"
      descricao="Horários de funcionamento, regras de agendamento e notificações — as mesmas configurações da loja, acessíveis pelo celular."
    />
  )
}
