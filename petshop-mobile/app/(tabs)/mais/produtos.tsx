import { EmBreve } from '@/components/EmBreve'

export default function ProdutosScreen() {
  return (
    <EmBreve
      titulo="Produtos"
      icone="cube-outline"
      descricao="Catálogo e estoque da loja, com as mesmas categorias, unidades de venda e alertas de estoque mínimo já usados no dashboard."
    />
  )
}
