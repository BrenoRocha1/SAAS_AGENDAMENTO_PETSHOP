import { EmBreve } from '@/components/EmBreve'

export default function FuncionariosScreen() {
  return (
    <EmBreve
      titulo="Funcionários"
      icone="people-circle-outline"
      descricao="Aqui vai a equipe da loja, com cargo e permissões — os mesmos dados da tela Equipe do dashboard web."
    />
  )
}
